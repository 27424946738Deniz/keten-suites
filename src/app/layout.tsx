import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Keten - Student Housing & Mid-Term Rentals",
  description:
    "Modern, fully furnished housing solutions for students and professionals in Istanbul. Flexible lease terms, prime locations, and exceptional amenities.",
  keywords: ["student housing", "mid-term rental", "Istanbul", "furnished apartments"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
